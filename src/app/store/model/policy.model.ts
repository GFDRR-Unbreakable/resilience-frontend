/**
 * The PolicyPriority data interface which determines what type of data will be the
 * text input fields on the Scorecard-Priority-List page.
 */
export interface PolicyPriority {
    firstCountry: string;
    secondCountry: string;
};
